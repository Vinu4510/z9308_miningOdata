_schema-version: "3.2"
ID: appminingdata
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appminingdata-destination-content
  type: com.sap.application.content
  requires:
  - name: appminingdata-destination-service
    parameters:
      content-target: true
  - name: appminingdata-repo-host
    parameters:
      service-key:
        name: appminingdata-repo-host-key
  - name: appminingdata-uaa
    parameters:
      service-key:
        name: appminingdata-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appminingdata_html_repo_host
          ServiceInstanceName: appminingdata-html5-service
          ServiceKeyName: appminingdata-repo-host-key
          sap.cloud.service: appminingdata
        - Authentication: OAuth2UserTokenExchange
          Name: appminingdata_uaa
          ServiceInstanceName: appminingdata-xsuaa-service
          ServiceKeyName: appminingdata-uaa-key
          sap.cloud.service: appminingdata
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appminingdata-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appminingdata-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appminingdata.zip
      name: appminingdata
      target-path: resources/
- name: appminingdata
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appminingdata-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appminingdata-destination-service
    service-plan: lite
- name: appminingdata-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appminingdata-xsuaa-service
    service-plan: application
- name: appminingdata-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appminingdata-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
